ЗВІТ ДО ЛАБОРАТОРНОЇ РОБОТИ №3

Тема: Робота з класами та об’єктами у Python
Мета роботи: Закріпити навички створення класів, конструкторів, методів, властивостей, роботи зі списками та збереження даних у файли.

 Виконання роботи

✔Результати виконання завдання 1N

i. Розробили/Створили
 • створено файл main.py;
 • розроблено клас User;
 • реалізовано конструктор init;
 • додано валідацію імені (лише літери);
 • реалізовано автоматичне форматування імені (велика літера);
 • реалізовано методи:
 • say_hello();
 • count_letters();
 • create_email(domain);
 • save_to_file();
 • додано властивість full_name;
 • створено список користувачів та додано своє ім’я;
 • додано функцію створення “анонімного” користувача.

![Опис зображення](pictures/L3.png)


![Опис зображення](pictures/L3.2.png) 

ii. Програма вивела значення
 • коректне привітання користувача;
 • кількість літер імені;
 • email з обраним доменом;
 • структуру списку імен;
 • інформацію у форматі "User #id: Name (email)";
 • запис користувача у файл.


iii. Отримано наступні результати
 • список імен успішно обробляється;
 • некоректні імена викликають ValueError;
 • дані зберігаються у users.txt;
 • email формується динамічно з вибором домену;
 • кількість літер імені рахується правильно.


iv. Навчились
 • створювати класи та методи у Python;
 • працювати з властивостями;
 • обробляти помилки через raise;
 • працювати зі списками об’єктів;
 • створювати Markdown-звіти;
 • завантажувати файли у GitHub;
 • вставляти скріншоти локальними шляхами.

 Висновок
 • Що зроблено в роботі:
Створено клас, реалізовано методи, валідацію, роботу з файлами, властивості, списки та повністю виконано лабораторне завдання.
 • Чи досягнуто мети роботи:
Так, мета виконана повністю.
 • Які нові знання отримано:
ООП у Python, робота з методами, валідація, файли, списки об’єктів, Markdown.
 • Чи вдалось відповісти на всі питання:
Так, всі питання опрацьовано.
 • Чи вдалося виконати всі завдання:
Так, виконано всі обов’язкові та індивідуальні завдання.
 • Чи виникли складності:
Виникали труднощі з запуском файлів, але всі вирішені.
 • Чи подобається формат здачі роботи:
Так, формат зручний, допомагає наводити порядок у проєкті.
 • Побажання:
 Немає